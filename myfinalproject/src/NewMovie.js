//this will the upload of new movies
import React from "react";
import styled from "styled-components";

const NewMovie = () => {
  // it will not work anymore
  return (
    <>
      <input type="file" />
    </>
  );
};

export default NewMovie;
